<Application x:Class="CateringIS.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:CateringIS.ViewModels"
             xmlns:v="clr-namespace:CateringIS.Views"
             xmlns:conv="clr-namespace:CateringIS.Converters"
             xmlns:helpers="clr-namespace:CateringIS.Helpers"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <conv:BoolToVisibilityConverter  x:Key="BoolToVis"/>
            <conv:NullToVisibilityConverter  x:Key="NullToVis"/>
            <conv:PathToImageConverter       x:Key="PathToImg"/>
            <conv:DecimalFormatConverter     x:Key="DecFmt"/>

            <!-- DataTemplate mapping: each ViewModel → its View -->
            <DataTemplate DataType="{x:Type vm:SuppliersViewModel}">
                <v:SuppliersView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:IngredientsViewModel}">
                <v:IngredientsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ProductGroupsViewModel}">
                <v:ProductGroupsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ProductsViewModel}">
                <v:ProductsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:DeliveriesViewModel}">
                <v:DeliveriesView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:DepartmentsViewModel}">
                <v:DepartmentsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:OrdersViewModel}">
                <v:OrdersView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SalesViewModel}">
                <v:SalesView/>
            </DataTemplate>

            <!-- ── Global Styles ─────────────────────────────────────────── -->
            <Style TargetType="Button" x:Key="NavBtn">
                <Setter Property="Background"   Value="#2C3E50"/>
                <Setter Property="Foreground"   Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding"      Value="12,8"/>
                <Setter Property="Margin"       Value="0,2"/>
                <Setter Property="FontSize"     Value="13"/>
                <Setter Property="Cursor"       Value="Hand"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#3D5166"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="Button" x:Key="ActionBtn">
                <Setter Property="Background"   Value="#27AE60"/>
                <Setter Property="Foreground"   Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding"      Value="10,6"/>
                <Setter Property="Margin"       Value="2"/>
                <Setter Property="FontSize"     Value="12"/>
                <Setter Property="Cursor"       Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#2ECC71"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="Button" x:Key="DangerBtn" BasedOn="{StaticResource ActionBtn}">
                <Setter Property="Background" Value="#E74C3C"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#C0392B"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="Button" x:Key="SecondaryBtn" BasedOn="{StaticResource ActionBtn}">
                <Setter Property="Background" Value="#2980B9"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#3498DB"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Padding"  Value="4,3"/>
                <Setter Property="Margin"   Value="0,2"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="BorderBrush" Value="#BDC3C7"/>
                <Setter Property="Validation.ErrorTemplate">
                    <Setter.Value>
                        <ControlTemplate>
                            <DockPanel>
                                <Border BorderBrush="Red" BorderThickness="2" CornerRadius="2">
                                    <AdornedElementPlaceholder/>
                                </Border>
                                <TextBlock Foreground="Red" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                           Text="{Binding [0].ErrorContent}"/>
                            </DockPanel>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="Validation.HasError" Value="True">
                        <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="#555"/>
                <Setter Property="Padding" Value="0,4,0,0"/>
            </Style>
            <Style TargetType="DataGrid">
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="CanUserAddRows"      Value="False"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="RowBackground"       Value="White"/>
                <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
                <Setter Property="BorderBrush"         Value="#DEE2E6"/>
                <Setter Property="FontSize"            Value="13"/>
                <Setter Property="SelectionMode"       Value="Single"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
