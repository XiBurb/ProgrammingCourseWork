<UserControl x:Class="CateringIS.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:CateringIS.Helpers">
    <Grid Margin="16" Cursor="Hand">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- List -->
        <DockPanel Grid.Column="0" Margin="0,0,12,0">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="+ Добавить запись" Command="{Binding AddCommand}"    Style="{StaticResource ActionBtn}"/>
                <Button Content="🗑 Удалить"         Command="{Binding DeleteCommand}" Style="{StaticResource DangerBtn}"/>
                <Button Content="💾 Сохранить"       Command="{Binding SaveCommand}"   Style="{StaticResource SecondaryBtn}"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding SalesRecords}" SelectedItem="{Binding Selected}" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата"       Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Блюдо"      Binding="{Binding ProductName}"  Width="*"/>
                    <DataGridTextColumn Header="Объём продаж" Binding="{Binding SalesVolume}" Width="110"/>
                    <DataGridTextColumn Header="Ед. изм."   Binding="{Binding Unit}"         Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Detail -->
        <Border Grid.Column="1" Background="White" CornerRadius="6"
                BorderBrush="#DEE2E6" BorderThickness="1" Padding="16"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Редактирование записи" FontWeight="Bold" FontSize="14" Margin="0,0,0,12"/>

                    <Label Content="Дата"/>
                    <DatePicker SelectedDate="{Binding Selected.Date}" Margin="0,2"/>

                    <Label Content="Блюдо"/>
                    <ComboBox ItemsSource="{Binding Products}"
                              SelectedValue="{Binding Selected.ProductId}"
                              SelectedValuePath="Id"
                              DisplayMemberPath="Name"
                              Padding="4,3" Margin="0,2"/>

                    <Label Content="Объём продаж"/>
                    <TextBox>
                        <TextBox.Text>
                            <Binding Path="Selected.SalesVolume" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <helpers:PositiveNumberValidationRule AllowZero="False"/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>

                    <Label Content="Единица измерения"/>
                    <TextBox>
                        <TextBox.Text>
                            <Binding Path="Selected.Unit" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <helpers:UnitValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <Button Content="💾 Сохранить"
        Click="SaveButton_Click"
        Style="{StaticResource SecondaryBtn}"
        Margin="0,12,0,0"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
