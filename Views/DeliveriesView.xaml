<UserControl x:Class="CateringIS.Views.DeliveriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:CateringIS.Helpers">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="180"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*" MinHeight="200"/>
        </Grid.RowDefinitions>

        <!-- ─── Top: Deliveries list ─────────────────────────────────────── -->
        <DockPanel Grid.Row="0">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="+ Новый приход" Command="{Binding AddCommand}"    Style="{StaticResource ActionBtn}"/>
                <Button Content="🗑 Удалить"      Command="{Binding DeleteCommand}" Style="{StaticResource DangerBtn}"/>
                <Button Content="💾 Сохранить"    Click="SaveButton_Click"   Style="{StaticResource SecondaryBtn}"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Deliveries}" SelectedItem="{Binding Selected}" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№"          Binding="{Binding Id}"             Width="50"/>
                    <DataGridTextColumn Header="Дата"       Binding="{Binding DeliveryDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Поставщик"  Binding="{Binding Supplier.Name}"  Width="*"/>
                    <DataGridTextColumn Header="Позиций"    Binding="{Binding Items.Count}"    Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <GridSplitter Grid.Row="1" Height="8" HorizontalAlignment="Stretch" Background="#DEE2E6"/>

        <!-- ─── Bottom: Edit selected delivery ──────────────────────────── -->
        <Border Grid.Row="2" Background="White" CornerRadius="6"
                BorderBrush="#DEE2E6" BorderThickness="1" Padding="12"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Состав прихода" FontWeight="Bold" FontSize="13" Margin="0,0,0,8"/>

                <WrapPanel Grid.Row="1" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                        <Label Content="Дата:" VerticalAlignment="Center"/>
                        <DatePicker SelectedDate="{Binding Selected.DeliveryDate}" Width="130"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Поставщик:" VerticalAlignment="Center"/>
                        <ComboBox ItemsSource="{Binding Suppliers}" SelectedItem="{Binding Selected.Supplier}"
                                  DisplayMemberPath="Name" Width="200" Padding="4,3"/>
                    </StackPanel>
                    <Button Content="+ Строка" Command="{Binding AddItemCommand}"
                            Style="{StaticResource ActionBtn}" Margin="16,0,0,0"/>
                </WrapPanel>

                <DataGrid Grid.Row="2" ItemsSource="{Binding DeliveryItems}" CanUserAddRows="False"
                          AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Ингредиент" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding IngredientName}" Padding="4,2"/></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.Ingredients,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              SelectedValue="{Binding IngredientId, UpdateSourceTrigger=PropertyChanged}"
                                              SelectedValuePath="Id" DisplayMemberPath="Name"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Ед. изм."       Binding="{Binding Unit}"          Width="80"/>
                        <DataGridTemplateColumn Header="Цена закупки" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding PurchasePrice}" Padding="4,2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox>
                                        <TextBox.Text>
                                            <Binding Path="PurchasePrice" UpdateSourceTrigger="PropertyChanged">
                                                <Binding.ValidationRules>
                                                    <helpers:PositiveNumberValidationRule AllowZero="False"/>
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Количество" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Quantity}" Padding="4,2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox>
                                        <TextBox.Text>
                                            <Binding Path="Quantity" UpdateSourceTrigger="PropertyChanged">
                                                <Binding.ValidationRules>
                                                    <helpers:PositiveNumberValidationRule AllowZero="False" FieldName="Количество"/>
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="" Width="30">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✕" Background="Transparent" BorderThickness="0"
                                            Foreground="#E74C3C" FontWeight="Bold" Cursor="Hand"
                                            Command="{Binding DataContext.RemoveItemCommand,
                                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
